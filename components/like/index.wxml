<!--components/like/index.wxml-->
<view class="container" bindtap="onToggleLike">
    <image class="like-img" src="{{likeStatus? likeUrl : unlikeUrl}}"></image>
    <text class="count" wx:if="{{showing}}">{{likeCount}}</text>
</view>
